name: "Stage deployment"

on:
  push:
    branches: ["stage"]

env:
  IS_CICD: "true"

permissions:
  contents: read

jobs:
  call-workflow-in-local-repo:
    uses: ./.github/workflows/deployment.yml
    with:
      environment: "stage"
